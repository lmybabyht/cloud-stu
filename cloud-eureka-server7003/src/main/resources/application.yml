server:
  port: 7003
eureka:
  instance:
    hostname: eureka7003.com #eureka 服务端实列
  client:
    # 自身不注册
    register-with-eureka: false
    # 不需要检索服务
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/
